# `.claude/output/`

Skill が生成した成果物（spec, PR 説明書き, 調査レポート等）を保存するディレクトリ。

各 skill は `<skill-name>/<YYYY-MM-DD-HHmm>-<topic>.md` 形式で書き出す。

中身は git 管轄外（このディレクトリの `.gitignore` で除外）。チームで共有したい成果物は別途 `docs/` 等に移すこと。
